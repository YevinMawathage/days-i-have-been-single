---
layout: base.njk
title: Days I've Been Single
---

{% set startDate = "2025-04-25" %}
{% set dayCount = startDate | daysSince %}

<div class="min-h-screen flex items-center justify-center p-6">
    <div class="max-w-4xl w-full">
        <!-- Terminal-style Card -->
        <div class="bg-gray-900 rounded-xl shadow-2xl overflow-hidden border border-gray-700">
            <!-- Terminal Header -->
            <div class="bg-gray-800 px-4 py-3 flex items-center gap-2 border-b border-gray-700">
                <div class="flex gap-2">
                    <div class="w-3 h-3 rounded-full bg-red-500"></div>
                    <div class="w-3 h-3 rounded-full bg-yellow-500"></div>
                    <div class="w-3 h-3 rounded-full bg-green-500"></div>
                </div>
                <div class="ml-2 text-gray-400 text-sm font-mono">~/single-counter.sh</div>
            </div>

            <!-- Terminal Content -->
            <div class="p-4 sm:p-8 md:p-12 font-mono text-left space-y-6">
                <!-- Configuration Grid -->
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
                    <!-- Date Input Block -->
                    <div class="bg-gray-800/50 border border-gray-700 rounded p-4 hover:border-green-500 transition-colors group relative overflow-hidden">
                        <div class="flex items-center gap-2 mb-2 relative z-10">
                            <svg class="w-4 h-4 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                            </svg>
                            <div class="text-xs text-gray-500 uppercase tracking-wider font-mono">const start_date</div>
                        </div>
                        <div class="relative z-10">
                            <input 
                                type="date" 
                                id="date-input" 
                                onchange="saveDateParts()" 
                                class="w-full bg-transparent text-xl font-bold text-green-400 focus:outline-none [&::-webkit-calendar-picker-indicator]:invert [&::-webkit-calendar-picker-indicator]:opacity-50 [&::-webkit-calendar-picker-indicator]:hover:opacity-100 cursor-pointer font-mono"
                            >
                        </div>
                    </div>

                    <!-- Time Input Block -->
                    <div class="bg-gray-800/50 border border-gray-700 rounded p-4 hover:border-blue-500 transition-colors group relative overflow-hidden">
                        <div class="flex items-center gap-2 mb-2 relative z-10">
                            <svg class="w-4 h-4 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                            <div class="text-xs text-gray-500 uppercase tracking-wider font-mono">const start_time</div>
                        </div>
                        <div class="relative z-10">
                            <input 
                                type="time" 
                                id="time-input" 
                                onchange="saveDateParts()" 
                                class="w-full bg-transparent text-xl font-bold text-blue-400 focus:outline-none [&::-webkit-calendar-picker-indicator]:invert [&::-webkit-calendar-picker-indicator]:opacity-50 [&::-webkit-calendar-picker-indicator]:hover:opacity-100 cursor-pointer font-mono"
                            >
                        </div>
                    </div>
                </div>

                <div class="flex justify-end mb-8">
                    <button onclick="resetToNow()" class="flex items-center gap-2 px-4 py-2 bg-gray-800 hover:bg-gray-700 text-gray-300 hover:text-white text-sm font-mono rounded-lg border border-gray-700 transition-all shadow-lg hover:shadow-cyan-500/20">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
                        </svg>
                        <span>Reset to Now</span>
                    </button>
                </div>

                <!-- Output -->
                <div class="space-y-4 text-gray-300">
                    <div class="my-8 p-6 bg-gray-800/50 border border-gray-700 rounded-lg">
                        <div class="text-center">
                            <div class="text-gray-400 text-sm mb-2">DAYS_SINGLE =</div>
                            <div id="days-counter" class="text-4xl sm:text-6xl md:text-8xl font-bold text-green-400">
                                {{ dayCount }}
                            </div>
                        </div>
                    </div>

                    <!-- Stats Grid -->
                    <div class="mt-8 grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4">
                        <div class="bg-gray-800/50 border border-gray-700 rounded p-4 hover:border-cyan-500 transition-colors">
                            <div class="flex items-center gap-2 mb-3">
                                <svg class="w-4 h-4 text-cyan-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                                </svg>
                                <div class="text-xs text-gray-500 uppercase tracking-wider">const hours</div>
                            </div>
                            <div id="hours-counter" class="text-2xl font-bold text-cyan-400">{{ dayCount * 24 }}</div>
                        </div>
                        <div class="bg-gray-800/50 border border-gray-700 rounded p-4 hover:border-blue-500 transition-colors">
                            <div class="flex items-center gap-2 mb-3">
                                <svg class="w-4 h-4 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                                </svg>
                                <div class="text-xs text-gray-500 uppercase tracking-wider">const minutes</div>
                            </div>
                            <div id="minutes-counter" class="text-2xl font-bold text-blue-400">{{ dayCount * 1440 }}</div>
                        </div>
                        <div class="bg-gray-800/50 border border-gray-700 rounded p-4 hover:border-purple-500 transition-colors">
                            <div class="flex items-center gap-2 mb-3">
                                <svg class="w-4 h-4 text-purple-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z"></path>
                                </svg>
                                <div class="text-xs text-gray-500 uppercase tracking-wider">const seconds</div>
                            </div>
                            <div id="seconds-counter" class="text-2xl font-bold text-purple-400">{{ dayCount * 86400 }}</div>
                        </div>
                    </div>

                    <!-- Additional Info -->
                    <div class="mt-6 space-y-2 text-sm">
                        <div class="text-gray-500">
                            <span class="text-gray-600">//</span> That's approximately <span id="weeks-counter">{{ (dayCount / 7) | round(1) }}</span> weeks, <span id="months-counter">{{ (dayCount / 30.44) | round(1) }}</span> months, or <span id="years-counter">{{ (dayCount / 365.25) | round(1) }}</span> years
                        </div>
                    </div>

                    <!-- Status Line -->
                    <div class="mt-8 pt-4 border-t border-gray-700">
                        <div class="flex items-center gap-2 text-green-400">
                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                            <span id="status-text">Status: Still going strong</span>
                        </div>
                    </div>

                    <!-- Prompt -->
                    <div class="text-green-400 flex items-center">
                        <span class="text-blue-400">$</span>
                        <span class="inline-block w-2 h-4 bg-green-400 ml-1 animate-blink"></span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Built with -->
        <div class="mt-6 text-center text-gray-500 text-xs font-mono">
            <div class="flex items-center justify-center gap-3 flex-wrap">
                <span class="text-gray-600">Built with</span>
                <a href="https://www.11ty.dev/" target="_blank" rel="noopener noreferrer" class="text-cyan-400 hover:text-cyan-300 transition-colors flex items-center gap-1.5">
                    <svg class="w-4 h-4" viewBox="0 0 24 24" fill="currentColor">
                        <rect x="3" y="3" width="7" height="7" rx="1"/>
                        <rect x="14" y="3" width="7" height="7" rx="1"/>
                        <rect x="3" y="14" width="7" height="7" rx="1"/>
                        <rect x="14" y="14" width="7" height="7" rx="1"/>
                        <rect x="10" y="10" width="4" height="4" rx="0.5"/>
                    </svg>
                    <span>Eleventy</span>
                </a>
                <span class="text-gray-600">+</span>
                <a href="https://tailwindcss.com/" target="_blank" rel="noopener noreferrer" class="text-cyan-400 hover:text-cyan-300 transition-colors flex items-center gap-1.5">
                    <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 6c-2.67 0-4.33 1.33-5 4 1-1.33 2.17-1.83 3.5-1.5.76.19 1.31.74 1.91 1.35.98 1 2.12 2.15 4.59 2.15 2.67 0 4.33-1.33 5-4-1 1.33-2.17 1.83-3.5 1.5-.76-.19-1.31-.74-1.91-1.35C15.61 7.15 14.47 6 12 6zm-5 6c-2.67 0-4.33 1.33-5 4 1-1.33 2.17-1.83 3.5-1.5.76.19 1.31.74 1.91 1.35.98 1 2.12 2.15 4.59 2.15 2.67 0 4.33-1.33 5-4-1 1.33-2.17 1.83-3.5 1.5-.76-.19-1.31-.74-1.91-1.35-1-1-2.12-2.15-4.59-2.15z"/>
                    </svg>
                    <span>Tailwind CSS</span>
                </a>
            </div>
        </div>

        <!-- Footer -->
        <div class="mt-4 text-center text-gray-500 text-sm font-mono flex items-center justify-center gap-2">
            <span class="text-gray-600">&lt;/&gt;</span>
            <span>with</span>
            <svg class="w-3 h-3 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"></path>
            </svg>
            <span>by</span>
            <a href="https://www.instagram.com/dfansoo/" target="_blank" rel="noopener noreferrer" class="text-cyan-400 hover:text-cyan-300 transition-colors flex items-center gap-1.5">
                <span>DFanso</span>
                <svg class="w-3.5 h-3.5" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/>
                </svg>
            </a>
        </div>
    </div>
</div>

<script>
    // Start date: Default or from LocalStorage
    let startDate = new Date();
    
    const savedDate = localStorage.getItem('singleStartDate');
    if (savedDate) {
        startDate = new Date(savedDate);
    }

    // Initialize inputs
    const dateInput = document.getElementById('date-input');
    const timeInput = document.getElementById('time-input');
    const pad = (n) => n < 10 ? '0' + n : n;

    const updateInputDisplay = () => {
        if (dateInput && timeInput) {
            const year = startDate.getFullYear();
            const month = pad(startDate.getMonth() + 1);
            const day = pad(startDate.getDate());
            const hours = pad(startDate.getHours());
            const minutes = pad(startDate.getMinutes());
            
            dateInput.value = `${year}-${month}-${day}`;
            timeInput.value = `${hours}:${minutes}`;
        }
    };
    
    // Call on load
    updateInputDisplay();

    function saveDateParts() {
        const dVal = document.getElementById('date-input').value;
        const tVal = document.getElementById('time-input').value;
        
        if (dVal && tVal) {
            startDate = new Date(`${dVal}T${tVal}`);
            localStorage.setItem('singleStartDate', startDate.toISOString());
            updateCounters();
        }
    }
    
    function resetToNow() {
        startDate = new Date();
        localStorage.setItem('singleStartDate', startDate.toISOString());
        updateInputDisplay();
        updateCounters();
    }
    
    function updateCounters() {
        const now = new Date();
        const diffMs = Math.max(0, now - startDate);
        
        // Calculate time units
        const days = Math.floor(diffMs / (1000 * 60 * 60 * 24));
        const hours = Math.floor(diffMs / (1000 * 60 * 60));
        const minutes = Math.floor(diffMs / (1000 * 60));
        const seconds = Math.floor(diffMs / 1000);
        
        const weeks = (days / 7).toFixed(1);
        const months = (days / 30.44).toFixed(1);
        const years = (days / 365.25).toFixed(1);
        
        // Update DOM elements
        document.getElementById('days-counter').textContent = days.toLocaleString();
        document.getElementById('hours-counter').textContent = hours.toLocaleString();
        document.getElementById('minutes-counter').textContent = minutes.toLocaleString();
        document.getElementById('seconds-counter').textContent = seconds.toLocaleString();
        document.getElementById('weeks-counter').textContent = weeks;
        document.getElementById('months-counter').textContent = months;
        document.getElementById('years-counter').textContent = years;
        
        updateStatus(days);
    }
    
    function updateStatus(days) {
        const statusElement = document.getElementById('status-text');
        let status = "Status: Just started";
        
        if (days >= 1825) { // 5 years
            status = "Status: Ascended Being (5+ Years)";
        } else if (days >= 1095) { // 3 years
            status = "Status: Monk Mode Activated (3+ Years)";
        } else if (days >= 730) { // 2 years
            status = "Status: Legendary Solo Player (2+ Years)";
        } else if (days >= 365) { // 1 year
            status = "Status: Master of My Destiny (1+ Year)";
        } else if (days >= 180) { // 6 months
            status = "Status: Independent & Thriving (6+ Months)";
        } else if (days >= 90) { // 3 months
            status = "Status: Finding Inner Peace (3+ Months)";
        } else if (days >= 30) { // 1 month
            status = "Status: Getting Used To It (1+ Month)";
        } else if (days >= 7) { // 1 week
            status = "Status: Freshly Single (1+ Week)";
        }
        
        statusElement.textContent = status;
    }
    
    // Update immediately
    updateCounters();
    
    // Update every second
    setInterval(updateCounters, 1000);
</script>
